import stdlib::http::*;
import stdlib::http::router::*;
import stdlib::http::route::*;

@get("/health")
fn first() -> HttpResponse { return HttpResponse::ok().text("a"); }

@get("/health")
fn second() -> HttpResponse { return HttpResponse::ok().text("b"); }

@listen(8080)
fn main() {}
