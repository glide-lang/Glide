// Single `.free()` at the very end -> no use after, no error.

fn main() {
    let arena: *Arena = Arena::new(64);
    let p: *i32 = arena.alloc(sizeof(i32)) as *i32;
    *p = 42;
    println!(*p);
    arena.free();
}
