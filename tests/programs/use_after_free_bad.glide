// Reading a value after `.free()` -> compile error.

fn main() {
    let arena: *Arena = Arena::new(64);
    arena.free();
    let used: i32 = arena.used();   // ERROR: use-after-free
    println!(used);
}
