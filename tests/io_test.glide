import stdlib::testing::*;
import stdlib::io::*;
import stdlib::bytes::*;

fn test_io_write_does_not_hang() -> i32 {
    io_write("");
    return 0;
}

fn test_byte_buffer_round_trip_bytes() -> i32 {
    let buf: *ByteBuffer = ByteBuffer::new();
    defer buf.free();

    buf.push(65 as u8);
    buf.push(0 as u8);
    buf.push(255 as u8);

    assert_eq!(buf.len(), 3);
    assert_eq!(buf.at(0) as i32, 65);
    assert_eq!(buf.at(1) as i32, 0);
    assert_eq!(buf.at(2) as i32, 255);
    return 0;
}

fn test_byte_buffer_string_round_trip() -> i32 {
    let buf: *ByteBuffer = ByteBuffer::new();
    defer buf.free();

    buf.push_str("abc");
    buf.push(68 as u8);
    assert_eq!(buf.len(), 4);
    assert_str_eq!(buf.as_string(), "abcD");
    return 0;
}

fn test_flush_does_not_hang() -> i32 {
    flush();
    return 0;
}
